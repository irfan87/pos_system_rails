<div class="navbar navbar-static-top navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<button class="navbar-toggle collapsed" data-target=".navbar-responsive-collapse" data-toggle="collapse" type="button">
				<span class="sr-only">Toggle Navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to "Docket System", root_path, class: "navbar-brand" %>
		</div>
		<div class="navbar-collapse collapse navbar-responsive-collapse">
			<% if current_user.present? %>
			<ul class="nav navbar-nav">
				<li><%= link_to '<span class="glyphicon glyphicon-list-alt"></span> PROFILE'.html_safe, user_path(current_user) %></li>
				<li class="disabled"><%= link_to '<span class="glyphicon glyphicon-envelope"></span> MESSAGE'.html_safe, "#" %></li>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
						<span class="glyphicon glyphicon-wrench"></span>
						SETTINGS
						<strong class="caret"></strong>
					</a>
					<ul class="dropdown-menu">
						<li>
							<%= link_to 'Edit Profile', edit_user_path(@user) %>
						</li>
						<li class="divider"></li>
						<li class="dropdown-header">Shops</li>
						<li>
							<%= link_to 'Shops List', user_shops_path(@user) %>
						</li>
						<li>
							<%= link_to 'Add New Shop', new_user_shop_path(@user) %>
						</li>
					</ul>
				</li>
			</ul>
			<ul class="nav navbar-nav navbar navbar-right">
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
						<span class="glyphicon glyphicon-user"></span>
						<%= current_user.username.upcase %>
						<span class="caret"></span>
					</a>
					<ul class="dropdown-menu">
						<li>
							<%= link_to '<span class="glyphicon glyphicon-log-out"></span> Log Out'.html_safe, logout_path%>
						</li>
					</ul>
				</li>
			</ul>
			<% else %>
			<ul class="nav navbar-nav navbar-right">
				<li>
					<%= link_to '<span class="glyphicon glyphicon-plus"></span> Sign Up'.html_safe, signup_path %>
				</li>
				<li>
					<%= link_to '<span class="glyphicon glyphicon-log-in"></span> Sign In'.html_safe, signin_path %>
				</li>
			</ul>
			<% end %>
		</div>
	</div>
</div>